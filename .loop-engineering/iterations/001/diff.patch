diff --git a/.loop-engineering/iterations/001/context.md b/.loop-engineering/iterations/001/context.md
index 91fbd6f..01f3dfa 100644
--- a/.loop-engineering/iterations/001/context.md
+++ b/.loop-engineering/iterations/001/context.md
@@ -1,12 +1,17 @@
 # Context
 
-- objective: Harden ReaderSignal into a production-ready, universally useful Kujo-native production Kujo tool without weakening its authority boundaries.
-- git_head: c5558bb
+- objective: Clean up, verify, and prepare ReaderSignal for the v0.2.0 release without weakening its authority boundaries.
+- git_head: 4524637
 - changed_files:
+  - .loop-engineering/loop.yml
   - CHANGELOG.md
-  - src/common.kujo
+  - README.md
+  - SECURITY.md
+  - VERSION
+  - kujo.toml
+  - schemas/record.schema.json
+  - scripts/validate.sh
   - src/core.kujo
-  - src/domain.kujo
+  - src/profile.kujo
   - src/storage.kujo
-  - tests/domain_test.kujo
-  - tests/security_test.kujo
+  - tests/test.kujo
diff --git a/.loop-engineering/iterations/001/diff.patch b/.loop-engineering/iterations/001/diff.patch
index 097cd66..e69de29 100644
--- a/.loop-engineering/iterations/001/diff.patch
+++ b/.loop-engineering/iterations/001/diff.patch
@@ -1,698 +0,0 @@
-diff --git a/.loop-engineering/iterations/001/context.md b/.loop-engineering/iterations/001/context.md
-index bb0a026..91fbd6f 100644
---- a/.loop-engineering/iterations/001/context.md
-+++ b/.loop-engineering/iterations/001/context.md
-@@ -1,11 +1,12 @@
- # Context
- 
- - objective: Harden ReaderSignal into a production-ready, universally useful Kujo-native production Kujo tool without weakening its authority boundaries.
--- git_head: 2537817
-+- git_head: c5558bb
- - changed_files:
-   - CHANGELOG.md
--  - README.md
--  - cli.kujo
--  - fixtures/core.json
-+  - src/common.kujo
-   - src/core.kujo
--  - tests/test.kujo
-+  - src/domain.kujo
-+  - src/storage.kujo
-+  - tests/domain_test.kujo
-+  - tests/security_test.kujo
-diff --git a/.loop-engineering/iterations/001/diff.patch b/.loop-engineering/iterations/001/diff.patch
-index b7144f6..e69de29 100644
---- a/.loop-engineering/iterations/001/diff.patch
-+++ b/.loop-engineering/iterations/001/diff.patch
-@@ -1,432 +0,0 @@
--diff --git a/CHANGELOG.md b/CHANGELOG.md
--index 546877a..e08b4e8 100644
----- a/CHANGELOG.md
--+++ b/CHANGELOG.md
--@@ -1,5 +1,9 @@
-- # Changelog
-- 
--+## Unreleased
--+
--+- Added privacy-bounded domain contracts, compatible-window comparisons, immutable storage, structured failures, CI, and expanded verification.
--+
-- ## 0.1.0 - 2026-08-14
-- 
-- - Initial Kujo-native release with working local records, validation, contracts, fixtures, and safety boundaries.
--diff --git a/README.md b/README.md
--index 8e9dd44..7af1e21 100644
----- a/README.md
--+++ b/README.md
--@@ -1,15 +1,61 @@
-- # ReaderSignal
-- 
---Audience-response and editorial-learning records for measurements, feedback, attribution limits, comparisons, and follow-up recommendations.
--+[![Version](https://img.shields.io/badge/version-0.1.0-black)](VERSION)
--+[![CI](https://github.com/kujolang/readersignal/actions/workflows/validate.yml/badge.svg)](https://github.com/kujolang/readersignal/actions/workflows/validate.yml)
--+[![License](https://img.shields.io/badge/license-MIT-lightgrey)](LICENSE)
--+[![built with Kujo](https://img.shields.io/badge/built%20with-Kujo-white.svg)](https://github.com/kujolang/kujo)
-- 
---ReaderSignal 0.1.0 is an independently installable, local-first Kujo tool. It requires no hosted service, Chain of Command, WebOps, or sibling Publishing House tool. The canonical entrypoint is `readersignal.kujo`; `bin/readersignal` contains no product logic.
--+ReaderSignal is a local-first Kujo tool for privacy-bounded measurement snapshots, feedback, comparisons, and evidence-linked learning. It has no required hosted service, database server, model key, or sibling-tool dependency.
-- 
---## CLI
--+## Readiness posture
-- 
---Commands: snapshot; provider add; sync; feedback add; compare; signals; learn; followup; report; export; doctor; version; init; show; validate; history. Run `./bin/readersignal help` for flags. Mutations require `--actor`; JSON input uses `--input`. Common flags include `--json`, `--dry-run`, `--state`, `--output`, `--config`, and `--force`. Exit codes: 0 success, 1 validation/operation failure, 2 usage error.
--+ReaderSignal is ready for serious standalone workflows: immutable records, append-only audit events, atomic writes, per-record locks, bounded inputs and queries, structured errors, deterministic fixtures, strict domain contracts, and explicit authority boundaries. Optional external capabilities fail honestly when no adapter is configured. It does not claim hosted identity or distributed multi-host coordination.
-- 
---State defaults to `.readersignal/`. Immutable JSON records and append-only history use atomic writes. IDs reject traversal; symlinks and oversized inputs are rejected. See [contracts](docs/contracts.md), [security](docs/security.md), and [quickstart](examples/quickstart.md).
--+See the [production review](docs/PRODUCTION_READINESS_REVIEW.md) and [next-session worklist](docs/NEXT_SESSION.md).
-- 
---Test with `/Users/robertdevore/2026/Kujolang/kujo-repos/kujo/target/release/kujo run tests/test.kujo`, then run `./bin/readersignal doctor --json`.
--+## Quick install
-- 
---0.1.0 covers the documented local records, fixtures, validation, checksums, deterministic fixed-time IDs, and structured export. It does not manufacture human judgment, consent, rights, approval, or causation. 0.1.0 supports manual JSON snapshots, deterministic fixtures, and PressWire receipt imports; live analytics providers are unavailable optional adapters, and correlation is never represented as causation.
--+Requires Kujo 1.0.1 or newer.
--+
--+```bash
--+git clone https://github.com/kujolang/readersignal.git
--+cd readersignal
--+export KUJO_BIN=/absolute/path/to/kujo
--+export PATH="$PWD/bin:$PATH"
--+readersignal --version --json
--+readersignal doctor --json
--+```
--+
--+## Quick start
--+
--+```bash
--+readersignal init --state .readersignal --json
--+readersignal snapshot --input fixtures/core.json --actor analyst --json
--+readersignal validate --json
--+readersignal export --output readersignal-export.json --json
--+```
--+
--+Run `readersignal --help` for the complete command surface. Common flags include `--state`, `--config`, `--input`, `--actor`, `--timestamp`, `--id`, `--path`, `--type`, `--after`, `--limit`, `--output`, `--force`, `--dry-run`, and `--json`. JSON mode uses the stable `ok/data/error/error_code/tool_version/contract_version` envelope. Exit codes are 0 success, 1 operational failure, and 2 usage error.
--+
--+State defaults to `.readersignal/`. Traversal, symlinks, secret-shaped fields, malformed JSON, incompatible schemas, duplicate IDs, checksum drift, oversized resources, and unsafe overwrites fail closed. Core behavior is implemented entirely in Kujo; adapters remain optional.
--+
--+## Project structure
--+
--+```text
--+readersignal.kujo       canonical entrypoint
--+src/                  CLI, domain, storage, and shared Kujo modules
--+tests/                regression, security, and domain suites
--+schemas/              public JSON contracts
--+fixtures/             deterministic offline inputs
--+scripts/              validation gates
--+docs/                 contracts, security, review, and future work
--+bin/readersignal        logic-free launcher
--+```
--+
--+## Verification
--+
--+```bash
--+bash scripts/validate.sh
--+```
--+
--+The gate checks the entrypoint, every Kujo suite, JSON artifacts, CLI smoke paths, foreign-runtime boundaries, and the Git diff.
--diff --git a/cli.kujo b/cli.kujo
--deleted file mode 100644
--index e019b06..0000000
----- a/cli.kujo
--+++ /dev/null
--@@ -1,74 +0,0 @@
---func normalize_flag_name(token) {
---    if starts_with(token, "--") {
---        return substring(token, 2, len(token))
---    }
---    return token
---}
---
---func contains_name(names, name) {
---    mut i := 0
---    while i < len(names) {
---        if names[i] == name {
---            return true
---        }
---        i = i + 1
---    }
---    return false
---}
---
---func parse_args(tokens, bool_flags) {
---    mut flags := {}
---    mut positionals := []
---    mut errors := []
---    mut i := 0
---    while i < len(tokens) {
---        token := tokens[i]
---        if token == "--" {
---            i = i + 1
---            while i < len(tokens) {
---                positionals = push(positionals, tokens[i])
---                i = i + 1
---            }
---            continue
---        }
---        if starts_with(token, "--") {
---            eq_index := index_of(token, "=")
---            if eq_index >= 0 {
---                name := normalize_flag_name(substring(token, 0, eq_index))
---                if contains_name(bool_flags, name) {
---                    errors = push(errors, "--" + name + " does not take a value")
---                } else if has_key(flags, name) == 1 {
---                    errors = push(errors, "duplicate flag: --" + name)
---                } else {
---                    flags[name] := substring(token, eq_index + 1, len(token))
---                }
---                i = i + 1
---                continue
---            }
---            name := normalize_flag_name(token)
---            if has_key(flags, name) == 1 {
---                errors = push(errors, "duplicate flag: --" + name)
---                i = i + 1
---                continue
---            }
---            if contains_name(bool_flags, name) {
---                flags[name] := true
---                i = i + 1
---                continue
---            }
---            if i + 1 < len(tokens) && starts_with(tokens[i + 1], "--") == false {
---                flags[name] := tokens[i + 1]
---                i = i + 2
---                continue
---            }
---            flags[name] := ""
---            i = i + 1
---            continue
---        }
---        positionals = push(positionals, token)
---        i = i + 1
---    }
---    return {"flags": flags, "positionals": positionals, "errors": errors}
---}
---
---export parse_args := parse_args
--diff --git a/fixtures/core.json b/fixtures/core.json
--index 5c2c191..9f2e9f3 100644
----- a/fixtures/core.json
--+++ b/fixtures/core.json
--@@ -1 +1 @@
---{"schema_version":"1.0.0","title":"ReaderSignal fixture","provenance":[{"kind":"fixture","source":"fixtures/core.json"}],"classification":"observed","verdict":"unverified","blocking_findings":[],"human_actor":"fixture-human","package_id":"package-fixture","package_version":"1","manifest_checksum":"fixture-checksum","destination":"fixture-local","action":"publish","approval_id":"approval-fixture","authorized_action":"publish"}
--+{"schema_version":"1.0.0","publication_receipt_id":"receipt-fixture","window":"2026-08","metrics":{"views":10},"measurement_status":"observed","attribution_limit":"correlation only","provenance":["fixture"]}
--diff --git a/src/core.kujo b/src/core.kujo
--index 2706b7e..bb73bdd 100644
----- a/src/core.kujo
--+++ b/src/core.kujo
--@@ -1,43 +1,185 @@
---from cli import parse_args
---SLUG:="readersignal";NAME:="ReaderSignal";VERSION:="0.1.0";CONTRACT:="1.0.0";DEFAULT_STATE:=".readersignal";PREFIX:="measurement";COMMANDS:=split("snapshot,provider add,sync,feedback add,compare,signals,learn,followup,report,export,doctor,version,init,show,validate,history",",")
---func get(d,k,f){if type(d)=="dict"&&has_key(d,k)==1{return d[k]};return f}
---func has(a,v){for x in a{if x==v{return true}};return false}
---func flag(p,k,f){if has_key(p["flags"],k)==1{return p["flags"][k]};return f}
---func safe(v){return type(v)=="string"&&v!=""&&v!="."&&v!=".."&&contains(v,"/")==0&&contains(v,"\\")==0}
---func parent(p){mut i:=len(p)-1;while i>=0{if substring(p,i,i+1)=="/"{if i==0{return "/"};return substring(p,0,i)};i=i-1};return "."}
---func tree(p){if p==""||p=="."{return};parts:=split(p,"/");cur:="";if starts_with(p,"/"){cur="/"};for part in parts{if part!=""&&part!="."{if cur=="/"{cur="/"+part}else if cur==""{cur=part}else {cur=cur+"/"+part};if !path_is_dir(cur){create_dir(cur)}}}}
---func atomic(p,t){tree(parent(p));tmp:=p+".tmp-"+to_string(now());write_file(tmp,t,true);rename_file(tmp,p)}
---func canonical(v){if type(v)=="dict"{a:=[];for k in sort(keys(v)){a=push(a,to_json(k)+":"+canonical(v[k]))};return "{"+join(a,",")+"}"};if type(v)=="array"{a:=[];for x in v{a=push(a,canonical(x))};return "["+join(a,",")+"]"};return to_json(v)}
---func state(p){return flag(p,"state",DEFAULT_STATE)}
---func path(p,id){return join_path(join_path(state(p),"records"),id+".json")}
---func init(p){d:=state(p);if file_exists(d)&&path_is_symlink(d){return {"ok":false,"error":"state directory symlinks are not allowed"}};tree(join_path(d,"records"));tree(join_path(d,"history"));m:=join_path(d,"metadata.json");if !path_is_file(m){atomic(m,to_json_pretty({"schema_version":"1.0.0","tool":SLUG,"tool_version":VERSION,"created_at":now_utc()}))};return {"ok":true,"value":{"initialized":true,"state":d}}}
---func parse_object(text){mut v:=null;try {v=parse_json(text)} except err {return {"ok":false,"error":"malformed JSON"}};if type(v)!="dict"{return {"ok":false,"error":"input must be a JSON object"}};return {"ok":true,"value":v}}
---func payload(p){f:=flag(p,"input","");if f==""{return {"ok":true,"value":{}}};if !path_is_file(f){return {"ok":false,"error":"input file not found"}};if path_is_symlink(f)||file_size(f)>1048576{return {"ok":false,"error":"unsafe or oversized input"}};parsed:=parse_object(read_file(f));if !parsed["ok"]{return parsed};v:=parsed["value"];s:=get(v,"schema_version","1.0.0");if type(s)!="string"||!starts_with(s,"1."){return {"ok":false,"error":"incompatible schema_version"}};return {"ok":true,"value":v}}
---func domain(c,v,p){if SLUG=="dossier"&&(c=="evidence classify"||c=="verify"){cl:=get(v,"classification","");if cl!=""&&!has(["verified","observed","inferred","opinion","hypothesis","planned","conflicted","expired","unavailable","rejected"],cl){return "invalid evidence classification"};if cl=="verified"&&get(v,"captured_support","")==""{return "verified evidence requires captured support"}};if SLUG=="bluepencil"&&c=="review"{ver:=get(v,"verdict","unverified");if !has(["pass","pass_with_queries","revise","blocked","reject","unverified"],ver){return "invalid verdict"};if len(get(v,"blocking_findings",[]))>0&&(ver=="pass"||ver=="pass_with_queries"){return "blockers cannot be averaged away"}};if SLUG=="versionseal"&&c=="approve"{for k in ["human_actor","package_id","package_version","manifest_checksum","destination","action"]{if get(v,k,"")==""{return "approval missing "+k}};if flag(p,"actor","")!=v["human_actor"]{return "actor must match human_actor"}};if SLUG=="presswire"&&has(["publish","schedule","correct","unpublish"],c){if flag(p,"act",false)!=true||flag(p,"yes",false)!=true{return "effects require --act --yes"};if get(v,"approval_id","")==""||get(v,"manifest_checksum","")==""{return "approval evidence required"};if get(v,"authorized_action","")!=c{return "approval action mismatch"}};return ""}
---func reader_guard(c,v){if c=="compare"&&get(v,"window_a","")!=get(v,"window_b",""){return "incompatible measurement windows"};if (c=="learn"||c=="followup")&&get(v,"attribution_limit","")==""{return "editorial learning requires attribution_limit"};return ""}
---func kind(c){a:=split(c," ");return a[0]}
---func idprefix(c){if SLUG=="storydesk"{if kind(c)=="idea"{return "idea"};if kind(c)=="campaign"{return "campaign"};if kind(c)=="commission"{return "brief"};if c=="assign"{return "assignment"};if c=="handoff"{return "handoff"}};if SLUG=="dossier"&&has(["claim","source","evidence","rights"],kind(c)){return kind(c)};if SLUG=="galleypack"{if c=="build"||c=="freeze"{return "package"};return "artifact"};if SLUG=="bluepencil"{return "review"};if SLUG=="versionseal"{return "approval"};if SLUG=="presswire"{return "publication"};if SLUG=="readersignal"{if c=="learn"||c=="followup"{return "learning"};return "measurement"};if SLUG=="assetworks"{return "asset"};return PREFIX}
---func create(c,p){i:=init(p);if !i["ok"]{return i};x:=payload(p);if !x["ok"]{return x};v:=x["value"];e:=domain(c,v,p);if e==""{e=reader_guard(c,v)};if e!=""{return {"ok":false,"error":e}};actor:=flag(p,"actor","");if actor==""{return {"ok":false,"error":"mutating commands require --actor"}};ts:=flag(p,"timestamp",now_utc());pre:=idprefix(c);id:=flag(p,"id","");if id==""{id=pre+"-"+substring(sha256(SLUG+"\n"+c+"\n"+actor+"\n"+ts+"\n"+canonical(v)),0,20)};if !safe(id)||!starts_with(id,pre+"-"){return {"ok":false,"error":"invalid id prefix"}};dest:=path(p,id);if path_is_file(dest){return {"ok":false,"error":"duplicate record id"}};r:={"schema_version":"1.0.0","tool_version":VERSION,"id":id,"record_type":kind(c),"command":c,"actor":actor,"created_at":ts,"provenance":get(v,"provenance",[]),"payload":v};artifact:=flag(p,"path","");if artifact!=""{if !path_is_file(artifact)||path_is_symlink(artifact)||file_size(artifact)>67108864{return {"ok":false,"error":"unsafe artifact"}};r["artifact"]={"path":artifact,"sha256":sha256_file(artifact),"bytes":file_size(artifact)}};if flag(p,"dry-run",false)==true{return {"ok":true,"value":{"dry_run":true,"record":r}}};atomic(dest,to_json_pretty(r));atomic(join_path(state(p),"history/"+substring(sha256(ts+id),0,24)+".json"),to_json_pretty({"schema_version":"1.0.0","event":"record.created","record_id":id,"actor":actor,"at":ts,"checksum":sha256(canonical(r))}));return {"ok":true,"value":r}}
---func load(p,id){if !safe(id){return {"ok":false,"error":"unsafe record id"}};f:=path(p,id);if !path_is_file(f)||path_is_symlink(f){return {"ok":false,"error":"record not found"}};v:=parse_json(read_file(f));if type(v)!="dict"{return {"ok":false,"error":"malformed record"}};return {"ok":true,"value":v}}
---func list(p){a:=[];d:=join_path(state(p),"records");if !path_is_dir(d){return a};for n in sort(list_dir(d)){if ends_with(n,".json")&&!path_is_symlink(join_path(d,n)){v:=parse_json(read_file(join_path(d,n)));if type(v)=="dict"{a=push(a,v)};if len(a)>=1000{return a}}};return a}
---func verify(p){l:=load(p,flag(p,"id",""));if !l["ok"]{return l};f:=flag(p,"path","");if f!=""{expected:=get(get(l["value"],"artifact",{}),"sha256",get(l["value"]["payload"],"manifest_checksum",""));if !path_is_file(f)||expected==""||sha256_file(f)!=expected{return {"ok":false,"error":"checksum drift detected"}}};return {"ok":true,"value":{"valid":true,"record_id":l["value"]["id"],"checksum":sha256(canonical(l["value"]))}}}
---export func dispatch(c,p){if c=="init"{return init(p)};if c=="version"{return {"ok":true,"value":{"tool":SLUG,"version":VERSION,"kujo_version":"1.0.1"}}};if c=="doctor"{return {"ok":true,"value":{"healthy":true,"state":state(p),"kujo_version":"1.0.1","limitations":"0.1.0 supports manual JSON snapshots, deterministic fixtures, and PressWire receipt imports; live analytics providers are unavailable optional adapters, and correlation is never represented as causation."}}};if c=="show"||c=="claim show"||c=="receipt"||c=="inspect"{return load(p,flag(p,"id",""))};if has(["validate","verify","freshness check","status"],c){return verify(p)};if has(["list","idea list","review-queue","history","report","signals","adapters","capabilities","disagreements"],c){a:=list(p);return {"ok":true,"value":{"records":a,"count":len(a)}}};if c=="export"{return {"ok":true,"value":{"schema_version":"1.0.0","tool":SLUG,"records":list(p)}}};return create(c,p)}
---func cname(a){if len(a)==0{return "help"};if len(a)>1&&has(COMMANDS,a[0]+" "+a[1]){return a[0]+" "+a[1]};return a[0]}
---func tokens(a,c){if contains(c," ")==1{return slice(a,2,len(a))};return slice(a,1,len(a))}
---func env(ok,data,error){return {"ok":ok,"data":data,"error":error,"tool_version":VERSION,"contract_version":CONTRACT}}
---export func main(a) {
---    c := cname(a)
---    if c == "help" { print(NAME + " " + VERSION + "\n\nCommands:\n  " + join(COMMANDS, "\n  ") + "\n\nFlags: --state --input --id --actor --timestamp --path --json --dry-run --output --config --force"); return 0 }
---    if !has(COMMANDS, c) { eprint(NAME + ": unknown command: " + c); return 2 }
---    p := parse_args(tokens(a, c), ["json", "dry-run", "force", "act", "yes"])
---    if len(p["errors"]) > 0 { eprint(NAME + ": " + p["errors"][0]); return 2 }
---    allowed := ["state", "input", "id", "actor", "timestamp", "path", "json", "dry-run", "output", "config", "force", "act", "yes"]
---    for k in keys(p["flags"]) { if !has(allowed, k) { eprint(NAME + ": unknown flag --" + k); return 2 } }
---    r := dispatch(c, p)
---    out := env(r["ok"], get(r, "value", null), get(r, "error", null))
---    if flag(p, "json", false) { print(to_json_pretty(out)) }
---    else if r["ok"] { print(NAME + ": ok\n" + to_json_pretty(get(r, "value", null))) }
---    else { eprint(NAME + ": " + r["error"]) }
---    if r["ok"] { return 0 }
--+from src.args import parse_args
--+from src.common import get, has, canonical_json, parse_object, safe_id, valid_timestamp, find_sensitive_key, bounded_int, envelope
--+from src.profile import profile
--+from src.domain import record_type, id_prefix, validate_command
--+from src.storage import initialize, save_new, load_record, list_records, validate_state_path
--+
--+func flag(parsed, name, fallback) {
--+    if has_key(parsed["flags"], name) == 1 { return parsed["flags"][name] }
--+    return fallback
--+}
--+
--+func command_name(argv, commands) {
--+    if len(argv) == 0 { return "help" }
--+    if argv[0] == "--help" { return "help" }
--+    if argv[0] == "--version" { return "version" }
--+    if len(argv) > 1 && has(commands, argv[0] + " " + argv[1]) { return argv[0] + " " + argv[1] }
--+    return argv[0]
--+}
--+
--+func command_tokens(argv, command) {
--+    if command == "help" || (len(argv) > 0 && starts_with(argv[0], "--")) { return [] }
--+    if contains(command, " ") == 1 { return slice(argv, 2, len(argv)) }
--+    return slice(argv, 1, len(argv))
--+}
--+
--+func load_config(path) {
--+    if path == "" { return {"ok": true, "value": {}} }
--+    if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "config_not_found", "error": "config must be a regular file"} }
--+    if file_size(path) > 65536 { return {"ok": false, "code": "config_too_large", "error": "config exceeds 64 KiB"} }
--+    config_result := parse_object(read_file(path), "config"); if !config_result["ok"] { return config_result }
--+    for key in keys(config_result["value"]) { if !has(["state", "actor", "limit"], key) { return {"ok": false, "code": "unknown_config_key", "error": "unknown config key: " + key} } }
--+    return config_result
--+}
--+
--+func setting(parsed, config, name, fallback) {
--+    value := flag(parsed, name, null)
--+    if type(value) != "null" && value != "" { return value }
--+    return get(config, name, fallback)
--+}
--+
--+func read_payload(parsed) {
--+    path := flag(parsed, "input", "")
--+    if path == "" { return {"ok": true, "value": {"schema_version": "1.0.0"}} }
--+    if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "input_not_found", "error": "input must be a regular file"} }
--+    if file_size(path) > 1048576 { return {"ok": false, "code": "input_too_large", "error": "input exceeds 1 MiB"} }
--+    parsed_payload := parse_object(read_file(path), "input"); if !parsed_payload["ok"] { return parsed_payload }
--+    payload := parsed_payload["value"]
--+    schema := get(payload, "schema_version", "1.0.0")
--+    if type(schema) != "string" || !starts_with(schema, "1.") { return {"ok": false, "code": "incompatible_schema", "error": "input schema major must be 1"} }
--+    sensitive := find_sensitive_key(payload, "")
--+    if sensitive != "" { return {"ok": false, "code": "secret_rejected", "error": "sensitive field is not allowed: " + sensitive} }
--+    return {"ok": true, "value": payload}
--+}
--+
--+func artifact_metadata(path) {
--+    if path == "" { return {"ok": true, "value": null} }
--+    if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "unsafe_artifact", "error": "artifact must be a regular non-symlink file"} }
--+    if file_size(path) > 67108864 { return {"ok": false, "code": "artifact_too_large", "error": "artifact exceeds 64 MiB"} }
--+    return {"ok": true, "value": {"path": path, "sha256": sha256_file(path), "bytes": file_size(path)}}
--+}
--+
--+func create_record(command, parsed, config, p) {
--+    state := setting(parsed, config, "state", p["default_state"])
--+    initialized := initialize(state, {"schema_version": "1.0.0", "tool": p["slug"], "tool_version": p["version"], "contract_version": p["contract_version"], "created_at": now_utc()})
--+    if !initialized["ok"] { return initialized }
--+    input := read_payload(parsed); if !input["ok"] { return input }; payload := input["value"]
--+    actor := setting(parsed, config, "actor", "")
--+    if type(actor) != "string" || trim(actor) == "" || len(actor) > 256 { return {"ok": false, "code": "actor_required", "error": "mutating commands require a bounded --actor or config actor"} }
--+    timestamp := flag(parsed, "timestamp", now_utc())
--+    if !valid_timestamp(timestamp) { return {"ok": false, "code": "invalid_timestamp", "error": "timestamp must be RFC 3339 UTC"} }
--+    artifact := artifact_metadata(flag(parsed, "path", "")); if !artifact["ok"] { return artifact }
--+    domain_error := validate_command(command, payload, artifact["value"])
--+    if domain_error != "" { code := "domain_validation"; if starts_with(domain_error, "unsupported:") { code = "unsupported" }; return {"ok": false, "code": code, "error": domain_error} }
--+    prefix := id_prefix(command); id := flag(parsed, "id", "")
--+    if id == "" { id = prefix + "-" + substring(sha256(p["slug"] + "\n" + command + "\n" + actor + "\n" + timestamp + "\n" + canonical_json(payload)), 0, 20) }
--+    if !safe_id(id) || !starts_with(id, prefix + "-") { return {"ok": false, "code": "unsafe_id", "error": "id must use prefix " + prefix + "- and contain only lowercase letters, digits, and hyphens"} }
--+    record := {"schema_version": "1.0.0", "tool_version": p["version"], "contract_version": p["contract_version"], "id": id, "record_type": record_type(command), "command": command, "actor": actor, "created_at": timestamp, "provenance": get(payload, "provenance", []), "payload": payload}
--+    if type(artifact["value"]) == "dict" { record["artifact"] = artifact["value"] }
--+    if flag(parsed, "dry-run", false) == true { return {"ok": true, "value": {"dry_run": true, "record": record}} }
--+    stored := save_new(state, record); if !stored["ok"] { return stored }
--+    return {"ok": true, "value": {"record": record, "storage": stored["value"]}}
--+}
--+
--+func validate_record(record, p) {
--+    for key in ["schema_version", "tool_version", "contract_version", "id", "record_type", "command", "actor", "created_at", "provenance", "payload"] {
--+        if has_key(record, key) == 0 { return {"ok": false, "code": "missing_field", "error": "record missing field: " + key} }
--+    }
--+    if record["schema_version"] != "1.0.0" || record["contract_version"] != p["contract_version"] { return {"ok": false, "code": "incompatible_record", "error": "record contract is incompatible"} }
--+    if !safe_id(record["id"]) || !valid_timestamp(record["created_at"]) { return {"ok": false, "code": "invalid_record", "error": "record id or timestamp is invalid"} }
--+    if type(record["payload"]) != "dict" || type(record["provenance"]) != "array" { return {"ok": false, "code": "invalid_record", "error": "payload/provenance shape is invalid"} }
--+    artifact := get(record, "artifact", null)
--+    if type(artifact) == "dict" {
--+        path := get(artifact, "path", "")
--+        if path == "" || !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "artifact_missing", "error": "bound artifact is missing or unsafe"} }
--+        if sha256_file(path) != get(artifact, "sha256", "") { return {"ok": false, "code": "checksum_drift", "error": "bound artifact checksum drift detected"} }
--+    }
--+    error := validate_command(record["command"], record["payload"], get(record, "artifact", null))
--+    if error != "" { return {"ok": false, "code": "domain_validation", "error": error} }
--+    return {"ok": true}
--+}
--+
--+func export_result(value, parsed, p) {
--+    output := flag(parsed, "output", "")
--+    if output == "" { return {"ok": true, "value": value} }
--+    if file_exists(output) && path_is_symlink(output) { return {"ok": false, "code": "unsafe_output", "error": "output symlinks are not allowed"} }
--+    if file_exists(output) && flag(parsed, "force", false) != true { return {"ok": false, "code": "output_exists", "error": "output exists; use --force only for intentional safe replacement"} }
--+    rendered := to_json_pretty(envelope(true, value, null, null, p["version"], p["contract_version"])) + "\n"
--+    if len(rendered) > 8388608 { return {"ok": false, "code": "output_too_large", "error": "output exceeds 8 MiB"} }
--+    write_file_atomic(output, rendered, true)
--+    return {"ok": true, "value": {"output": output, "bytes": len(rendered), "sha256": sha256(rendered)}}
--+}
--+
--+export func dispatch(command, parsed) {
--+    p := profile(); config_result := load_config(flag(parsed, "config", "")); if !config_result["ok"] { return config_result }; config := config_result["value"]
--+    state := setting(parsed, config, "state", p["default_state"])
--+    if command == "init" { return initialize(state, {"schema_version": "1.0.0", "tool": p["slug"], "tool_version": p["version"], "contract_version": p["contract_version"], "created_at": now_utc()}) }
--+    if command == "version" { return {"ok": true, "value": {"tool": p["slug"], "version": p["version"], "contract_version": p["contract_version"], "minimum_kujo": "1.0.1"}} }
--+    if command == "doctor" {
--+        state_error := validate_state_path(state); listing := {"records": [], "warnings": [], "truncated": false}
--+        if state_error == "" { listing = list_records(state, "", "", 1000) }
--+        healthy := state_error == "" && len(listing["warnings"]) == 0
--+        mut code := null; mut error := null
--+        if state_error != "" { code = "unsafe_state"; error = state_error }
--+        if state_error == "" && len(listing["warnings"]) > 0 { code = "corrupt_state"; error = "one or more records are unreadable" }
--+        return {"ok": healthy, "code": code, "error": error, "value": {"healthy": healthy, "tool": p["slug"], "version": p["version"], "minimum_kujo": "1.0.1", "state": state, "record_count": len(listing["records"]), "corrupt_records": listing["warnings"], "limits": {"input_bytes": 1048576, "record_bytes": 1048576, "artifact_bytes": 67108864, "list_records": 1000}, "limitations": p["limitations"]}}
--+    }
--+    if has(p["show_commands"], command) {
--+        id := flag(parsed, "id", ""); if id == "" { return {"ok": false, "code": "id_required", "error": "show requires --id"} }
--+        return load_record(state, id)
--+    }
--+    if has(p["list_commands"], command) {
--+        limit_result := bounded_int(setting(parsed, config, "limit", 100), 100, 1, 1000); if !limit_result["ok"] { return limit_result }
--+        kind := flag(parsed, "type", get(p["list_kinds"], command, ""))
--+        listing := list_records(state, kind, flag(parsed, "after", ""), limit_result["value"])
--+        return {"ok": true, "value": listing}
--+    }
--+    if has(p["diff_commands"], command) {
--+        left_id := flag(parsed, "id", ""); right_id := flag(parsed, "other-id", "")
--+        if left_id == "" || right_id == "" { return {"ok": false, "code": "ids_required", "error": "comparison requires --id and --other-id"} }
--+        left := load_record(state, left_id); if !left["ok"] { return left }
--+        right := load_record(state, right_id); if !right["ok"] { return right }
--+        compatibility_field := get(p,"comparison_compatibility_field","")
--+        if compatibility_field!="" && get(left["value"]["payload"],compatibility_field,null)!=get(right["value"]["payload"],compatibility_field,null) { return {"ok":false,"code":"incompatible_comparison","error":"records have incompatible "+compatibility_field} }
--+        left_artifact := get(left["value"], "artifact", {}); right_artifact := get(right["value"], "artifact", {})
--+        return {"ok": true, "value": {"left_id": left_id, "right_id": right_id, "records_equal": canonical_json(left["value"]["payload"]) == canonical_json(right["value"]["payload"]), "left_record_sha256": left["checksum"], "right_record_sha256": right["checksum"], "left_artifact_sha256": get(left_artifact, "sha256", null), "right_artifact_sha256": get(right_artifact, "sha256", null), "artifact_equal": get(left_artifact, "sha256", null) == get(right_artifact, "sha256", null)}}
--+    }
--+    if has(p["validate_commands"], command) {
--+        id := flag(parsed, "id", "")
--+        if id != "" { loaded := load_record(state, id); if !loaded["ok"] { return loaded }; checked := validate_record(loaded["value"], p); if !checked["ok"] { return checked }; return {"ok": true, "value": {"valid": true, "id": id, "checksum": loaded["checksum"]}} }
--+        listing := list_records(state, "", "", 1000); mut invalid := []
--+        for record in listing["records"] { checked := validate_record(record, p); if !checked["ok"] { invalid = push(invalid, {"id": record["id"], "code": checked["code"], "error": checked["error"]}) } }
--+        valid := len(invalid) == 0 && len(listing["warnings"]) == 0
--+        mut code := null; mut error := null
--+        if !valid { code = "validation_failed"; error = "one or more records failed validation" }
--+        return {"ok": valid, "code": code, "error": error, "value": {"valid": valid, "checked": len(listing["records"]), "invalid": invalid, "storage_warnings": listing["warnings"], "truncated": listing["truncated"]}}
--+    }
--+    if has(p["export_commands"], command) {
--+        limit_result := bounded_int(setting(parsed, config, "limit", 1000), 1000, 1, 1000); if !limit_result["ok"] { return limit_result }
--+        listing := list_records(state, flag(parsed, "type", ""), flag(parsed, "after", ""), limit_result["value"])
--+        return export_result({"schema_version": "1.0.0", "tool": p["slug"], "records": listing["records"], "warnings": listing["warnings"], "truncated": listing["truncated"]}, parsed, p)
--+    }
--+    return create_record(command, parsed, config, p)
--+}
--+
--+func usage(p) {
--+    return p["name"] + " " + p["version"] + "\n\n" + p["description"] + "\n\nCommands:\n  " + join(p["commands"], "\n  ") + "\n\nCommon flags:\n  --state PATH       Local state directory\n  --config FILE      JSON defaults for state, actor, and limit\n  --input FILE       JSON command payload (1 MiB maximum)\n  --actor ID         Required for mutations\n  --timestamp UTC    Deterministic RFC 3339 timestamp\n  --id ID            Explicit stable record ID\n  --path FILE        Bounded artifact attachment\n  --type TYPE        Record filter for list/export\n  --after ID         Pagination cursor\n  --limit N          1..1000 records\n  --output FILE      Atomic JSON export\n  --force            Replace only an explicitly named safe output\n  --dry-run          Validate without writing\n  --json             Stable output envelope"
--+}
--+
--+export func main(argv) {
--+    p := profile(); command := command_name(argv, p["commands"])
--+    if command == "help" { print(usage(p)); return 0 }
--+    if !has(p["commands"], command) { eprint(p["name"] + ": unknown command: " + command); return 2 }
--+    parsed := parse_args(command_tokens(argv, command), ["json", "dry-run", "force"])
--+    if len(parsed["errors"]) > 0 { eprint(p["name"] + ": " + parsed["errors"][0]); return 2 }
--+    allowed := ["state", "config", "input", "id", "other-id", "actor", "timestamp", "path", "type", "after", "limit", "output", "json", "dry-run", "force"]
--+    for name in keys(parsed["flags"]) { if !has(allowed, name) { eprint(p["name"] + ": unknown flag --" + name); return 2 } }
--+    if len(parsed["positionals"]) > 0 { eprint(p["name"] + ": unexpected positional argument: " + parsed["positionals"][0]); return 2 }
--+    result := dispatch(command, parsed)
--+    output := envelope(result["ok"], get(result, "value", null), get(result, "error", null), get(result, "code", null), p["version"], p["contract_version"])
--+    if flag(parsed, "json", false) { print(to_json_pretty(output)) }
--+    else if result["ok"] { print(p["name"] + ": ok\n" + to_json_pretty(get(result, "value", null))) }
--+    else { eprint(p["name"] + ": " + get(result, "error", "operation failed") + " [" + get(result, "code", "unknown") + "]") }
--+    if result["ok"] { return 0 }
--     return 1
-- }
--diff --git a/tests/test.kujo b/tests/test.kujo
--index c51ab14..8ecd8e6 100644
----- a/tests/test.kujo
--+++ b/tests/test.kujo
--@@ -1,12 +1,12 @@
-- from src.core import dispatch
---from cli import parse_args
--+from src.args import parse_args
-- mut pass:=0;mut fail:=0
-- func check(ok,label){if ok{pass=pass+1}else{fail=fail+1;eprint("FAIL: "+label)}}
-- root:="/tmp/readersignal-test-"+uuid_v4()
-- func opts(v){return parse_args(v,["json","dry-run","force","act","yes"])}
-- check(dispatch("init",opts(["--state",root]))["ok"],"init")
-- created:=dispatch("snapshot",opts(["--state",root,"--input","fixtures/core.json","--actor","fixture-human","--timestamp","2026-08-14T00:00:00Z"]))
---check(created["ok"],"core workflow");id:="";if created["ok"]{id=created["value"]["id"]}
--+check(created["ok"],"core workflow");id:="";if created["ok"]{id=created["value"]["record"]["id"]}
-- check(!dispatch("snapshot",opts(["--state",root,"--input","fixtures/core.json","--actor","fixture-human","--timestamp","2026-08-14T00:00:00Z"]))["ok"],"duplicate")
-- check(!dispatch("snapshot",opts(["--state",root,"--input","fixtures/core.json"]))["ok"],"actor required")
-- check(!dispatch("show",opts(["--state",root,"--id","../escape"]))["ok"],"traversal")
--@@ -19,6 +19,6 @@ check(dispatch("export",opts(["--state",root]))["value"]["schema_version"]=="1.0
-- if id!=""{check(dispatch("show",opts(["--state",root,"--id",id]))["ok"],"show")}
-- bad_compare:=root+"/bad-compare.json";write_file(bad_compare,"{\"schema_version\":\"1.0.0\",\"window_a\":\"7d\",\"window_b\":\"30d\"}",true)
-- check(!dispatch("compare",opts(["--state",root,"--input",bad_compare,"--actor","analyst"]))["ok"],"incompatible windows rejected")
---learning:=root+"/learning.json";write_file(learning,"{\"schema_version\":\"1.0.0\",\"attribution_limit\":\"correlation only\",\"recommendation\":\"investigate recurring question\"}",true)
--+learning:=root+"/learning.json";write_file(learning,"{\"schema_version\":\"1.0.0\",\"evidence_ids\":[\"snapshot-fixture\"],\"attribution_limit\":\"correlation only\",\"recommendation\":\"investigate recurring question\"}",true)
-- check(dispatch("followup",opts(["--state",root,"--input",learning,"--actor","analyst","--timestamp","2026-08-14T00:00:02Z"]))["ok"],"bounded follow-up recommendation")
-- print(to_string(pass)+" passed; "+to_string(fail)+" failed");if fail>0{exit(1)}
-diff --git a/CHANGELOG.md b/CHANGELOG.md
-index e08b4e8..0b1f918 100644
---- a/CHANGELOG.md
-+++ b/CHANGELOG.md
-@@ -2,6 +2,7 @@
- 
- ## Unreleased
- 
-+- Enforced bounded numeric metric maps and non-empty evidence sets alongside stronger state, timestamp, actor, pagination, and immutable-record validation.
- - Added privacy-bounded domain contracts, compatible-window comparisons, immutable storage, structured failures, CI, and expanded verification.
- 
- ## 0.1.0 - 2026-08-14
-diff --git a/src/common.kujo b/src/common.kujo
-index 24507cf..6f89c8e 100644
---- a/src/common.kujo
-+++ b/src/common.kujo
-@@ -34,12 +34,12 @@ export func safe_id(value) {
- }
- 
- export func valid_timestamp(value) {
--    return type(value) == "string" && regex_match(value, "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$")
-+    return type(value) == "string" && regex_match(value, "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$")
- }
- 
- func sensitive_name(name) {
-     normalized := lower(replace_str(replace_str(name, "-", "_"), " ", "_"))
--    return has(["password", "passwd", "secret", "token", "api_key", "apikey", "authorization", "private_key", "client_secret", "access_key"], normalized)
-+    return regex_match(normalized, "(^|_)(password|passwd|secret|token|api_key|apikey|authorization|private_key|client_secret|access_key)($|_)")
- }
- 
- export func find_sensitive_key(value, path) {
-diff --git a/src/core.kujo b/src/core.kujo
-index bb73bdd..53a3b6f 100644
---- a/src/core.kujo
-+++ b/src/core.kujo
-@@ -55,8 +55,9 @@ func read_payload(parsed) {
- func artifact_metadata(path) {
-     if path == "" { return {"ok": true, "value": null} }
-     if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "unsafe_artifact", "error": "artifact must be a regular non-symlink file"} }
--    if file_size(path) > 67108864 { return {"ok": false, "code": "artifact_too_large", "error": "artifact exceeds 64 MiB"} }
--    return {"ok": true, "value": {"path": path, "sha256": sha256_file(path), "bytes": file_size(path)}}
-+    bytes := file_size(path)
-+    if bytes > 67108864 { return {"ok": false, "code": "artifact_too_large", "error": "artifact exceeds 64 MiB"} }
-+    return {"ok": true, "value": {"path": path, "sha256": sha256_file(path), "bytes": bytes}}
- }
- 
- func create_record(command, parsed, config, p) {
-@@ -65,10 +66,11 @@ func create_record(command, parsed, config, p) {
-     if !initialized["ok"] { return initialized }
-     input := read_payload(parsed); if !input["ok"] { return input }; payload := input["value"]
-     actor := setting(parsed, config, "actor", "")
--    if type(actor) != "string" || trim(actor) == "" || len(actor) > 256 { return {"ok": false, "code": "actor_required", "error": "mutating commands require a bounded --actor or config actor"} }
-+    if type(actor) != "string" || trim(actor) == "" || len(actor) > 256 || !regex_match(actor, "^[^\\x00-\\x1F\\x7F]+$") { return {"ok": false, "code": "actor_required", "error": "mutating commands require a bounded printable --actor or config actor"} }
-     timestamp := flag(parsed, "timestamp", now_utc())
-     if !valid_timestamp(timestamp) { return {"ok": false, "code": "invalid_timestamp", "error": "timestamp must be RFC 3339 UTC"} }
-     artifact := artifact_metadata(flag(parsed, "path", "")); if !artifact["ok"] { return artifact }
-+    if type(get(payload, "provenance", [])) != "array" { return {"ok": false, "code": "invalid_provenance", "error": "provenance must be an array"} }
-     domain_error := validate_command(command, payload, artifact["value"])
-     if domain_error != "" { code := "domain_validation"; if starts_with(domain_error, "unsupported:") { code = "unsupported" }; return {"ok": false, "code": code, "error": domain_error} }
-     prefix := id_prefix(command); id := flag(parsed, "id", "")
-@@ -85,13 +87,15 @@ func validate_record(record, p) {
-     for key in ["schema_version", "tool_version", "contract_version", "id", "record_type", "command", "actor", "created_at", "provenance", "payload"] {
-         if has_key(record, key) == 0 { return {"ok": false, "code": "missing_field", "error": "record missing field: " + key} }
-     }
--    if record["schema_version"] != "1.0.0" || record["contract_version"] != p["contract_version"] { return {"ok": false, "code": "incompatible_record", "error": "record contract is incompatible"} }
--    if !safe_id(record["id"]) || !valid_timestamp(record["created_at"]) { return {"ok": false, "code": "invalid_record", "error": "record id or timestamp is invalid"} }
-+    if record["schema_version"] != "1.0.0" || record["contract_version"] != p["contract_version"] || record["tool_version"] != p["version"] { return {"ok": false, "code": "incompatible_record", "error": "record tool or contract version is incompatible"} }
-+    if !has(p["commands"], record["command"]) || record["record_type"] != record_type(record["command"]) { return {"ok": false, "code": "invalid_record", "error": "record command or type is invalid"} }
-+    if !safe_id(record["id"]) || !valid_timestamp(record["created_at"]) || type(record["actor"]) != "string" || trim(record["actor"]) == "" || len(record["actor"]) > 256 || !regex_match(record["actor"], "^[^\\x00-\\x1F\\x7F]+$") { return {"ok": false, "code": "invalid_record", "error": "record id, actor, or timestamp is invalid"} }
-     if type(record["payload"]) != "dict" || type(record["provenance"]) != "array" { return {"ok": false, "code": "invalid_record", "error": "payload/provenance shape is invalid"} }
-     artifact := get(record, "artifact", null)
-     if type(artifact) == "dict" {
-         path := get(artifact, "path", "")
-         if path == "" || !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "artifact_missing", "error": "bound artifact is missing or unsafe"} }
-+        if file_size(path) > 67108864 || get(artifact, "bytes", -1) != file_size(path) { return {"ok": false, "code": "artifact_size_drift", "error": "bound artifact size is invalid or has drifted"} }
-         if sha256_file(path) != get(artifact, "sha256", "") { return {"ok": false, "code": "checksum_drift", "error": "bound artifact checksum drift detected"} }
-     }
-     error := validate_command(record["command"], record["payload"], get(record, "artifact", null))
-@@ -106,7 +110,7 @@ func export_result(value, parsed, p) {
-     if file_exists(output) && flag(parsed, "force", false) != true { return {"ok": false, "code": "output_exists", "error": "output exists; use --force only for intentional safe replacement"} }
-     rendered := to_json_pretty(envelope(true, value, null, null, p["version"], p["contract_version"])) + "\n"
-     if len(rendered) > 8388608 { return {"ok": false, "code": "output_too_large", "error": "output exceeds 8 MiB"} }
--    write_file_atomic(output, rendered, true)
-+    try { write_file_atomic(output, rendered, true) } except error { return {"ok": false, "code": "write_failed", "error": "atomic export write failed"} }
-     return {"ok": true, "value": {"output": output, "bytes": len(rendered), "sha256": sha256(rendered)}}
- }
- 
-@@ -131,7 +135,9 @@ export func dispatch(command, parsed) {
-     if has(p["list_commands"], command) {
-         limit_result := bounded_int(setting(parsed, config, "limit", 100), 100, 1, 1000); if !limit_result["ok"] { return limit_result }
-         kind := flag(parsed, "type", get(p["list_kinds"], command, ""))
--        listing := list_records(state, kind, flag(parsed, "after", ""), limit_result["value"])
-+        after := flag(parsed, "after", ""); if after != "" && !safe_id(after) { return {"ok": false, "code": "unsafe_cursor", "error": "pagination cursor must be a safe record id"} }
-+        listing := list_records(state, kind, after, limit_result["value"])
-+        if len(listing["warnings"]) > 0 && listing["warnings"][0]["code"] == "unsafe_state" { return {"ok": false, "code": "unsafe_state", "error": listing["warnings"][0]["error"]} }
-         return {"ok": true, "value": listing}
-     }
-     if has(p["diff_commands"], command) {
-@@ -156,7 +162,9 @@ export func dispatch(command, parsed) {
-     }
-     if has(p["export_commands"], command) {
-         limit_result := bounded_int(setting(parsed, config, "limit", 1000), 1000, 1, 1000); if !limit_result["ok"] { return limit_result }
--        listing := list_records(state, flag(parsed, "type", ""), flag(parsed, "after", ""), limit_result["value"])
-+        after := flag(parsed, "after", ""); if after != "" && !safe_id(after) { return {"ok": false, "code": "unsafe_cursor", "error": "pagination cursor must be a safe record id"} }
-+        listing := list_records(state, flag(parsed, "type", ""), after, limit_result["value"])
-+        if len(listing["warnings"]) > 0 && listing["warnings"][0]["code"] == "unsafe_state" { return {"ok": false, "code": "unsafe_state", "error": listing["warnings"][0]["error"]} }
-         return export_result({"schema_version": "1.0.0", "tool": p["slug"], "records": listing["records"], "warnings": listing["warnings"], "truncated": listing["truncated"]}, parsed, p)
-     }
-     return create_record(command, parsed, config, p)
-diff --git a/src/domain.kujo b/src/domain.kujo
-index 4e80337..c971c48 100644
---- a/src/domain.kujo
-+++ b/src/domain.kujo
-@@ -1,4 +1,5 @@
- from src.common import get, has
- export func record_type(command){return command};export func id_prefix(command){return command}
--func required(payload,fields){for field in fields{if type(get(payload,field,null))=="null"||get(payload,field,"")==""{return field+" is required"}};return ""}
--export func validate_command(command,payload,artifact){if command=="snapshot"{e:=required(payload,["publication_receipt_id","window","metrics","measurement_status","attribution_limit"]);if e!=""{return e};if type(payload["metrics"])!="dict"{return "metrics must be an object"};return ""};if command=="feedback"{return required(payload,["publication_receipt_id","channel","summary","consent_basis"])};if has(["learn","followup"],command){return required(payload,["evidence_ids","recommendation","attribution_limit"])};return ""}
-+func required_text(payload,fields){for field in fields{value:=get(payload,field,"");if type(value)!="string"||trim(value)==""{return field+" must be non-empty text"};if len(value)>32768{return field+" exceeds 32 KiB"}};return ""}
-+func evidence_ids(value){if type(value)!="array"||len(value)==0||len(value)>1000{return false};for id in value{if type(id)!="string"||trim(id)==""||len(id)>128{return false}};return true}
-+export func validate_command(command,payload,artifact){if command=="snapshot"{e:=required_text(payload,["publication_receipt_id","window","measurement_status","attribution_limit"]);if e!=""{return e};metrics:=get(payload,"metrics",null);if type(metrics)!="dict"||len(keys(metrics))==0||len(keys(metrics))>256{return "metrics must be an object with 1..256 entries"};for key in keys(metrics){if len(key)>128||!has(["int","float"],type(metrics[key])){return "metric values must be numeric and keys bounded"}};return ""};if command=="feedback"{return required_text(payload,["publication_receipt_id","channel","summary","consent_basis"])};if has(["learn","followup"],command){e:=required_text(payload,["recommendation","attribution_limit"]);if e!=""{return e};if !evidence_ids(get(payload,"evidence_ids",null)){return "evidence_ids must contain 1..1000 bounded ids"}};return ""}
-diff --git a/src/storage.kujo b/src/storage.kujo
-index 330ad34..ff597b9 100644
---- a/src/storage.kujo
-+++ b/src/storage.kujo
-@@ -10,15 +10,18 @@ func parent_dir(path) {
- }
- 
- func ensure_tree(path) {
--    if path == "" || path == "." { return }
-+    if path == "" || path == "." { return "" }
-     parts := split(path, "/"); mut current := ""; if starts_with(path, "/") { current = "/" }
-     for part in parts {
-         if part != "" && part != "." {
--            if part == ".." { continue }
-+            if part == ".." { return "path traversal is not allowed" }
-             if current == "/" { current = "/" + part } else if current == "" { current = part } else { current = current + "/" + part }
--            if !path_is_dir(current) { create_dir(current) }
-+            if file_exists(current) {
-+                if !path_is_dir(current) { return "path component is not a directory: " + current }
-+            } else { try { create_dir(current) } except error { return "could not create directory: " + current } }
-         }
-     }
-+    return ""
- }
- 
- export func validate_state_path(path) {
-@@ -36,9 +39,15 @@ export func record_path(state, id) { return join_path(records_dir(state), id + "
- 
- export func initialize(state, metadata) {
-     error := validate_state_path(state); if error != "" { return {"ok": false, "code": "unsafe_state", "error": error} }
--    ensure_tree(records_dir(state)); ensure_tree(history_dir(state)); ensure_tree(locks_dir(state))
-+    for dir in [records_dir(state), history_dir(state), locks_dir(state)] { error = ensure_tree(dir); if error != "" { return {"ok": false, "code": "unsafe_state", "error": error} }; if path_is_symlink(dir) || !path_is_dir(dir) { return {"ok": false, "code": "unsafe_state", "error": "managed state paths must be regular directories"} } }
-     path := join_path(state, "metadata.json")
--    if !path_is_file(path) { write_file_atomic(path, to_json_pretty(metadata) + "\n", true) }
-+    if file_exists(path) {
-+        if path_is_symlink(path) || !path_is_file(path) { return {"ok": false, "code": "unsafe_state", "error": "metadata must be a regular non-symlink file"} }
-+        if file_size(path) > 65536 { return {"ok": false, "code": "metadata_too_large", "error": "metadata exceeds 64 KiB"} }
-+        parsed := parse_object(read_file(path), "state metadata"); if !parsed["ok"] { return parsed }
-+        existing := parsed["value"]
-+        if get(existing, "tool", "") != get(metadata, "tool", "") || get(existing, "contract_version", "") != get(metadata, "contract_version", "") { return {"ok": false, "code": "incompatible_state", "error": "state metadata belongs to an incompatible tool or contract"} }
-+    } else { write_file_atomic(path, to_json_pretty(metadata) + "\n", true) }
-     return {"ok": true, "value": {"state": state, "initialized": true}}
- }
- 
-@@ -61,10 +70,12 @@ export func save_new(state, record) {
-     if path_is_file(path) { release_lock(lock["path"]); return {"ok": false, "code": "duplicate_id", "error": "record already exists: " + id} }
-     if file_exists(path) && path_is_symlink(path) { release_lock(lock["path"]); return {"ok": false, "code": "symlink_escape", "error": "record symlinks are not allowed"} }
-     serialized := to_json_pretty(record) + "\n"
-+    if len(serialized) > 1048576 { release_lock(lock["path"]); return {"ok": false, "code": "record_too_large", "error": "serialized record exceeds 1 MiB"} }
-     try { write_file_atomic(path, serialized, true) } except error { release_lock(lock["path"]); return {"ok": false, "code": "write_failed", "error": "atomic record write failed"} }
-     checksum := sha256(serialized)
-     event := {"schema_version": "1.0.0", "event": "record.created", "record_id": id, "actor": record["actor"], "at": record["created_at"], "record_sha256": checksum}
-     event_path := join_path(history_dir(state), substring(sha256(record["created_at"] + "\n" + id), 0, 32) + ".json")
-+    if file_exists(event_path) { release_lock(lock["path"]); return {"ok": false, "code": "history_conflict", "error": "audit history event already exists"} }
-     try { write_file_atomic(event_path, to_json_pretty(event) + "\n", true) } except error { release_lock(lock["path"]); return {"ok": false, "code": "history_write_failed", "error": "record was saved but audit history write failed"} }
-     release_lock(lock["path"])
-     return {"ok": true, "value": {"path": path, "checksum": checksum}}
-@@ -72,17 +83,20 @@ export func save_new(state, record) {
- 
- export func load_record(state, id) {
-     if !safe_id(id) { return {"ok": false, "code": "unsafe_id", "error": "unsafe record id"} }
-+    state_error := validate_state_path(state); if state_error != "" { return {"ok": false, "code": "unsafe_state", "error": state_error} }
-+    dir := records_dir(state); if file_exists(dir) && (path_is_symlink(dir) || !path_is_dir(dir)) { return {"ok": false, "code": "unsafe_state", "error": "records path must be a regular directory"} }
-     path := record_path(state, id)
-     if !path_is_file(path) || path_is_symlink(path) { return {"ok": false, "code": "not_found", "error": "record not found: " + id} }
-     if file_size(path) > 1048576 { return {"ok": false, "code": "record_too_large", "error": "record exceeds 1 MiB"} }
--    parsed := parse_object(read_file(path), "record " + id); if !parsed["ok"] { return parsed }
-+    contents := read_file(path); parsed := parse_object(contents, "record " + id); if !parsed["ok"] { return parsed }
-     if get(parsed["value"], "id", "") != id { return {"ok": false, "code": "id_mismatch", "error": "record id does not match filename"} }
--    return {"ok": true, "value": parsed["value"], "checksum": sha256(read_file(path))}
-+    return {"ok": true, "value": parsed["value"], "checksum": sha256(contents)}
- }
- 
- export func list_records(state, kind, after, limit) {
-     mut records := []; mut warnings := []; mut truncated := false
--    dir := records_dir(state); if !path_is_dir(dir) { return {"records": records, "warnings": warnings, "truncated": false} }
-+    state_error := validate_state_path(state); if state_error != "" { return {"records": records, "warnings": [{"id": "", "code": "unsafe_state", "error": state_error}], "truncated": false} }
-+    dir := records_dir(state); if file_exists(dir) && (path_is_symlink(dir) || !path_is_dir(dir)) { return {"records": records, "warnings": [{"id": "", "code": "unsafe_state", "error": "records path must be a regular directory"}], "truncated": false} }; if !path_is_dir(dir) { return {"records": records, "warnings": warnings, "truncated": false} }
-     for name in sort(list_dir(dir)) {
-         if ends_with(name, ".json") {
-             id := substring(name, 0, len(name) - 5)
-@@ -90,9 +104,8 @@ export func list_records(state, kind, after, limit) {
-                 loaded := load_record(state, id)
-                 if loaded["ok"] {
-                     record := loaded["value"]
--                    if kind == "" || get(record, "record_type", "") == kind { records = push(records, record) }
-+                    if kind == "" || get(record, "record_type", "") == kind { if len(records) >= limit { truncated = true; break }; records = push(records, record) }
-                 } else { warnings = push(warnings, {"id": id, "code": loaded["code"], "error": loaded["error"]}) }
--                if len(records) >= limit { truncated = true; break }
-             }
-         }
-     }
-diff --git a/tests/domain_test.kujo b/tests/domain_test.kujo
-index 962a6c0..41e336e 100644
---- a/tests/domain_test.kujo
-+++ b/tests/domain_test.kujo
-@@ -2,5 +2,5 @@ from src.core import dispatch
- from src.args import parse_args
- mut pass:=0;mut fail:=0;func check(ok,label){if ok{pass=pass+1}else{fail=fail+1;eprint("FAIL: "+label)}};func opts(v){return parse_args(v,["json","dry-run","force"])};root:="/tmp/readersignal-domain-"+uuid_v4()
- a:=dispatch("snapshot",opts(["--state",root,"--input","fixtures/core.json","--actor","analyst","--id","snapshot-a","--timestamp","2026-08-14T00:00:00Z"]));check(a["ok"],"snapshot")
--other:=root+"-other";write_file(other,"{\"schema_version\":\"1.0.0\",\"publication_receipt_id\":\"r\",\"window\":\"2026-09\",\"metrics\":{},\"measurement_status\":\"observed\",\"attribution_limit\":\"correlation only\"}",true);b:=dispatch("snapshot",opts(["--state",root,"--input",other,"--actor","analyst","--id","snapshot-b","--timestamp","2026-08-14T00:00:01Z"]));check(b["ok"],"second snapshot");c:=dispatch("compare",opts(["--state",root,"--id","snapshot-a","--other-id","snapshot-b"]));check(!c["ok"]&&c["code"]=="incompatible_comparison","window compatibility")
-+other:=root+"-other";write_file(other,"{\"schema_version\":\"1.0.0\",\"publication_receipt_id\":\"receipt-other\",\"window\":\"2026-09\",\"metrics\":{\"views\":20},\"measurement_status\":\"observed\",\"attribution_limit\":\"correlation only\"}",true);b:=dispatch("snapshot",opts(["--state",root,"--input",other,"--actor","analyst","--id","snapshot-b","--timestamp","2026-08-14T00:00:01Z"]));check(b["ok"],"second snapshot");c:=dispatch("compare",opts(["--state",root,"--id","snapshot-a","--other-id","snapshot-b"]));check(!c["ok"]&&c["code"]=="incompatible_comparison","window compatibility")
- print(to_string(pass)+" passed; "+to_string(fail)+" failed");if fail>0{exit(1)}
-diff --git a/tests/security_test.kujo b/tests/security_test.kujo
-index 4c6e7a7..9b4f200 100644
---- a/tests/security_test.kujo
-+++ b/tests/security_test.kujo
-@@ -1,6 +1,9 @@
- from src.core import dispatch
- from src.args import parse_args
- mut pass:=0;mut fail:=0;func check(ok,label){if ok{pass=pass+1}else{fail=fail+1;eprint("FAIL: "+label)}};func opts(v){return parse_args(v,["json","dry-run","force"])};root:="/tmp/readersignal-security-"+uuid_v4()
--check(!dispatch("init",opts(["--state",root+"/../escape"]))["ok"],"state traversal");secret:=root+"-secret";write_file(secret,"{\"schema_version\":\"1.0.0\",\"token\":\"secret\"}",true);check(!dispatch("snapshot",opts(["--state",root,"--input",secret,"--actor","analyst"]))["ok"],"secret rejection");check(!dispatch("show",opts(["--state",root,"--id","../escape"]))["ok"],"record traversal")
-+check(!dispatch("init",opts(["--state",root+"/../escape"]))["ok"],"state traversal");secret:=root+"-secret";write_file(secret,"{\"schema_version\":\"1.0.0\",\"provider_token\":\"secret\"}",true);check(!dispatch("snapshot",opts(["--state",root,"--input",secret,"--actor","analyst"]))["ok"],"secret rejection");check(!dispatch("show",opts(["--state",root,"--id","../escape"]))["ok"],"record traversal")
-+check(!dispatch("snapshot",opts(["--state",root,"--input","fixtures/core.json","--actor","analyst","--timestamp","2026-13-40T25:61:61Z"]))["ok"],"timestamp range validation")
- created:=dispatch("snapshot",opts(["--state",root,"--input","fixtures/core.json","--actor","analyst","--id","snapshot-secure","--timestamp","2026-08-14T00:00:00Z"]));check(created["ok"],"secure create")
-+foreign_state:=root+"-foreign";check(dispatch("init",opts(["--state",foreign_state]))["ok"],"foreign state baseline");write_file(foreign_state+"/metadata.json","{\"schema_version\":\"1.0.0\",\"tool\":\"other-tool\",\"contract_version\":\"1.0.0\"}",true);check(!dispatch("init",opts(["--state",foreign_state]))["ok"],"incompatible state rejected")
-+symlink_state:=root+"-symlink";outside_state:=root+"-outside";create_dir(symlink_state);create_dir(outside_state);linked:=spawn_process(["ln","-s",outside_state,symlink_state+"/records"],{"timeout_ms":5000,"max_output_bytes":4096});if linked.exitcode==0{check(!dispatch("init",opts(["--state",symlink_state]))["ok"],"managed directory symlink rejected")}
- print(to_string(pass)+" passed; "+to_string(fail)+" failed");if fail>0{exit(1)}
diff --git a/.loop-engineering/loop.yml b/.loop-engineering/loop.yml
index 7dcddf9..96be387 100644
--- a/.loop-engineering/loop.yml
+++ b/.loop-engineering/loop.yml
@@ -1,4 +1,4 @@
-objective: "Harden ReaderSignal into a production-ready, universally useful Kujo-native production Kujo tool without weakening its authority boundaries."
+objective: "Clean up, verify, and prepare ReaderSignal for the v0.2.0 release without weakening its authority boundaries."
 checklist_file: ""
 max_iterations: 1
 max_no_progress: 1
@@ -10,12 +10,13 @@ allowed_actions:
   - run_tests
   - commit_local_changes
   - push_changes
+  - tag_release
+  - publish_release
 
 blocked_actions:
   - deploy
   - destructive_cleanup
   - production_config_change
-  - release_action
   - contract_major_change
 
 eval_gates:
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 0b1f918..7125a75 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,7 +1,9 @@
 # Changelog
 
-## Unreleased
+## 0.2.0 - 2026-08-14
 
+- Preserved validation compatibility with immutable 0.1.0 records while emitting 0.2.0 records.
+- Prevented audit-history conflicts from leaving partial records and added clean-retry regression coverage.
 - Enforced bounded numeric metric maps and non-empty evidence sets alongside stronger state, timestamp, actor, pagination, and immutable-record validation.
 - Added privacy-bounded domain contracts, compatible-window comparisons, immutable storage, structured failures, CI, and expanded verification.
 
diff --git a/README.md b/README.md
index 7af1e21..c3faf26 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,6 @@
 # ReaderSignal
 
-[![Version](https://img.shields.io/badge/version-0.1.0-black)](VERSION)
+[![Version](https://img.shields.io/badge/version-0.2.0-black)](VERSION)
 [![CI](https://github.com/kujolang/readersignal/actions/workflows/validate.yml/badge.svg)](https://github.com/kujolang/readersignal/actions/workflows/validate.yml)
 [![License](https://img.shields.io/badge/license-MIT-lightgrey)](LICENSE)
 [![built with Kujo](https://img.shields.io/badge/built%20with-Kujo-white.svg)](https://github.com/kujolang/kujo)
diff --git a/SECURITY.md b/SECURITY.md
index 14d3d40..cffc489 100644
--- a/SECURITY.md
+++ b/SECURITY.md
@@ -1,3 +1,3 @@
 # Security policy
 
-Report vulnerabilities through GitHub Security Advisories without credentials or private evidence. Version 0.1.x receives fixes. Local state is operator-controlled storage, not a multi-tenant authorization boundary.
+Report vulnerabilities through GitHub Security Advisories without credentials or private evidence. Version 0.2.x receives fixes. Local state is operator-controlled storage, not a multi-tenant authorization boundary.
diff --git a/VERSION b/VERSION
index 6e8bf73..0ea3a94 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-0.1.0
+0.2.0
diff --git a/kujo.toml b/kujo.toml
index 47a02f0..a604605 100644
--- a/kujo.toml
+++ b/kujo.toml
@@ -1,4 +1,4 @@
 [package]
 name = "readersignal"
-version = "0.1.0"
+version = "0.2.0"
 entry = "readersignal.kujo"
diff --git a/schemas/record.schema.json b/schemas/record.schema.json
index 289f3ac..ce16861 100644
--- a/schemas/record.schema.json
+++ b/schemas/record.schema.json
@@ -1 +1 @@
-{"$schema":"https://json-schema.org/draft/2020-12/schema","title":"ReaderSignal record","type":"object","required":["schema_version","tool_version","id","record_type","command","actor","created_at","provenance","payload"],"properties":{"schema_version":{"const":"1.0.0"},"tool_version":{"const":"0.1.0"},"id":{"type":"string"},"record_type":{"type":"string"},"command":{"type":"string"},"actor":{"type":"string"},"created_at":{"type":"string"},"provenance":{"type":"array"},"payload":{"type":"object"}},"additionalProperties":true}
+{"$schema":"https://json-schema.org/draft/2020-12/schema","title":"ReaderSignal record","type":"object","required":["schema_version","tool_version","id","record_type","command","actor","created_at","provenance","payload"],"properties":{"schema_version":{"const":"1.0.0"},"tool_version":{"const":"0.2.0"},"id":{"type":"string"},"record_type":{"type":"string"},"command":{"type":"string"},"actor":{"type":"string"},"created_at":{"type":"string"},"provenance":{"type":"array"},"payload":{"type":"object"}},"additionalProperties":true}
diff --git a/scripts/validate.sh b/scripts/validate.sh
index e39a5b4..7e82c0e 100755
--- a/scripts/validate.sh
+++ b/scripts/validate.sh
@@ -8,6 +8,7 @@ cd "$ROOT"
 "$KUJO_RUNTIME" check readersignal.kujo
 "$KUJO_RUNTIME" run tests/test.kujo
 "$KUJO_RUNTIME" run tests/security_test.kujo
+"$KUJO_RUNTIME" run tests/storage_test.kujo
 "$KUJO_RUNTIME" run tests/domain_test.kujo
 while IFS= read -r document; do "$KUJO_RUNTIME" run scripts/validate_json.kujo -- "$document"; done < <(find fixtures schemas -type f -name '*.json' -print | sort)
 tmp_state="$(mktemp -d)"; trap 'find "$tmp_state" -depth -delete' EXIT
diff --git a/src/core.kujo b/src/core.kujo
index 53a3b6f..11fe93e 100644
--- a/src/core.kujo
+++ b/src/core.kujo
@@ -87,7 +87,8 @@ func validate_record(record, p) {
     for key in ["schema_version", "tool_version", "contract_version", "id", "record_type", "command", "actor", "created_at", "provenance", "payload"] {
         if has_key(record, key) == 0 { return {"ok": false, "code": "missing_field", "error": "record missing field: " + key} }
     }
-    if record["schema_version"] != "1.0.0" || record["contract_version"] != p["contract_version"] || record["tool_version"] != p["version"] { return {"ok": false, "code": "incompatible_record", "error": "record tool or contract version is incompatible"} }
+    supported_versions := get(p, "record_versions", [p["version"]])
+    if record["schema_version"] != "1.0.0" || record["contract_version"] != p["contract_version"] || !has(supported_versions, record["tool_version"]) { return {"ok": false, "code": "incompatible_record", "error": "record tool or contract version is incompatible"} }
     if !has(p["commands"], record["command"]) || record["record_type"] != record_type(record["command"]) { return {"ok": false, "code": "invalid_record", "error": "record command or type is invalid"} }
     if !safe_id(record["id"]) || !valid_timestamp(record["created_at"]) || type(record["actor"]) != "string" || trim(record["actor"]) == "" || len(record["actor"]) > 256 || !regex_match(record["actor"], "^[^\\x00-\\x1F\\x7F]+$") { return {"ok": false, "code": "invalid_record", "error": "record id, actor, or timestamp is invalid"} }
     if type(record["payload"]) != "dict" || type(record["provenance"]) != "array" { return {"ok": false, "code": "invalid_record", "error": "payload/provenance shape is invalid"} }
@@ -118,7 +119,7 @@ export func dispatch(command, parsed) {
     p := profile(); config_result := load_config(flag(parsed, "config", "")); if !config_result["ok"] { return config_result }; config := config_result["value"]
     state := setting(parsed, config, "state", p["default_state"])
     if command == "init" { return initialize(state, {"schema_version": "1.0.0", "tool": p["slug"], "tool_version": p["version"], "contract_version": p["contract_version"], "created_at": now_utc()}) }
-    if command == "version" { return {"ok": true, "value": {"tool": p["slug"], "version": p["version"], "contract_version": p["contract_version"], "minimum_kujo": "1.0.1"}} }
+    if command == "version" { return {"ok": true, "value": {"tool": p["slug"], "version": p["version"], "contract_version": p["contract_version"], "supported_record_versions": get(p, "record_versions", [p["version"]]), "minimum_kujo": "1.0.1"}} }
     if command == "doctor" {
         state_error := validate_state_path(state); listing := {"records": [], "warnings": [], "truncated": false}
         if state_error == "" { listing = list_records(state, "", "", 1000) }
diff --git a/src/profile.kujo b/src/profile.kujo
index 9fc9824..ce4232a 100644
--- a/src/profile.kujo
+++ b/src/profile.kujo
@@ -1 +1 @@
-export func profile(){return {"name":"ReaderSignal","slug":"readersignal","version":"0.1.0","contract_version":"1.0.0","description":"Privacy-bounded reader measurement, comparison, feedback, and learning records.","default_state":".readersignal","commands":["init","snapshot","feedback","compare","learn","followup","report","history","show","validate","export","doctor","version"],"show_commands":["show"],"list_commands":["report","history"],"list_kinds":{},"validate_commands":["validate"],"export_commands":["export"],"diff_commands":["compare"],"comparison_compatibility_field":"window","limitations":["Measurement inputs must be supplied; ReaderSignal does not scrape providers.","Attribution is bounded and never represented as causal proof."]}}
+export func profile(){return {"name":"ReaderSignal","slug":"readersignal","version":"0.2.0","record_versions":["0.1.0","0.2.0"],"contract_version":"1.0.0","description":"Privacy-bounded reader measurement, comparison, feedback, and learning records.","default_state":".readersignal","commands":["init","snapshot","feedback","compare","learn","followup","report","history","show","validate","export","doctor","version"],"show_commands":["show"],"list_commands":["report","history"],"list_kinds":{},"validate_commands":["validate"],"export_commands":["export"],"diff_commands":["compare"],"comparison_compatibility_field":"window","limitations":["Measurement inputs must be supplied; ReaderSignal does not scrape providers.","Attribution is bounded and never represented as causal proof."]}}
diff --git a/src/storage.kujo b/src/storage.kujo
index ff597b9..42371d6 100644
--- a/src/storage.kujo
+++ b/src/storage.kujo
@@ -71,12 +71,17 @@ export func save_new(state, record) {
     if file_exists(path) && path_is_symlink(path) { release_lock(lock["path"]); return {"ok": false, "code": "symlink_escape", "error": "record symlinks are not allowed"} }
     serialized := to_json_pretty(record) + "\n"
     if len(serialized) > 1048576 { release_lock(lock["path"]); return {"ok": false, "code": "record_too_large", "error": "serialized record exceeds 1 MiB"} }
-    try { write_file_atomic(path, serialized, true) } except error { release_lock(lock["path"]); return {"ok": false, "code": "write_failed", "error": "atomic record write failed"} }
     checksum := sha256(serialized)
     event := {"schema_version": "1.0.0", "event": "record.created", "record_id": id, "actor": record["actor"], "at": record["created_at"], "record_sha256": checksum}
     event_path := join_path(history_dir(state), substring(sha256(record["created_at"] + "\n" + id), 0, 32) + ".json")
     if file_exists(event_path) { release_lock(lock["path"]); return {"ok": false, "code": "history_conflict", "error": "audit history event already exists"} }
-    try { write_file_atomic(event_path, to_json_pretty(event) + "\n", true) } except error { release_lock(lock["path"]); return {"ok": false, "code": "history_write_failed", "error": "record was saved but audit history write failed"} }
+    try { write_file_atomic(path, serialized, true) } except error { release_lock(lock["path"]); return {"ok": false, "code": "write_failed", "error": "atomic record write failed"} }
+    try { write_file_atomic(event_path, to_json_pretty(event) + "\n", true) } except error {
+        try { delete_file(path) } except rollback_error {}
+        release_lock(lock["path"])
+        if path_is_file(path) { return {"ok": false, "code": "history_write_failed", "error": "audit history write failed and record rollback was incomplete"} }
+        return {"ok": false, "code": "history_write_failed", "error": "audit history write failed; record write was rolled back"}
+    }
     release_lock(lock["path"])
     return {"ok": true, "value": {"path": path, "checksum": checksum}}
 }
diff --git a/tests/test.kujo b/tests/test.kujo
index 8ecd8e6..a89036d 100644
--- a/tests/test.kujo
+++ b/tests/test.kujo
@@ -14,9 +14,10 @@ bad:=root+"/bad.json";write_file(bad,"{",true);check(!dispatch("snapshot",opts([
 future:=root+"/future.json";write_file(future,"{\"schema_version\":\"2.0.0\"}",true);check(!dispatch("snapshot",opts(["--state",root,"--input",future,"--actor","fixture-human"]))["ok"],"schema major")
 check(dispatch("snapshot",opts(["--state",root,"--input","fixtures/core.json","--actor","fixture-human","--dry-run"]))["value"]["dry_run"],"dry run")
 check(dispatch("doctor",opts(["--state",root]))["value"]["healthy"],"doctor")
-check(dispatch("version",opts([]))["value"]["version"]=="0.1.0","version")
+check(dispatch("version",opts([]))["value"]["version"]=="0.2.0","version")
 check(dispatch("export",opts(["--state",root]))["value"]["schema_version"]=="1.0.0","export")
 if id!=""{check(dispatch("show",opts(["--state",root,"--id",id]))["ok"],"show")}
+if id!=""{record_path:=root+"/records/"+id+".json";legacy:=parse_json(read_file(record_path));legacy["tool_version"]="0.1.0";write_file(record_path,to_json_pretty(legacy)+"\n",true);check(dispatch("validate",opts(["--state",root,"--id",id]))["ok"],"0.1.0 record compatibility")}
 bad_compare:=root+"/bad-compare.json";write_file(bad_compare,"{\"schema_version\":\"1.0.0\",\"window_a\":\"7d\",\"window_b\":\"30d\"}",true)
 check(!dispatch("compare",opts(["--state",root,"--input",bad_compare,"--actor","analyst"]))["ok"],"incompatible windows rejected")
 learning:=root+"/learning.json";write_file(learning,"{\"schema_version\":\"1.0.0\",\"evidence_ids\":[\"snapshot-fixture\"],\"attribution_limit\":\"correlation only\",\"recommendation\":\"investigate recurring question\"}",true)
